<h1><%=@building.name%></h1>

<h3>Companies:</h3>
<ul>
    <%@building.companies.each do |company|%>
        <li><%=link_to company.name, company_path(company)%></li><br>
    <%end%>
</ul>

<h4><%="Total Rent: #{@building.rent_per_floor * @building.companies.count}"%></h4>